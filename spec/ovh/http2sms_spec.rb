# frozen_string_literal: true

RSpec.describe Ovh::Http2sms do
  it "has a version number" do
    expect(Ovh::Http2sms::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
